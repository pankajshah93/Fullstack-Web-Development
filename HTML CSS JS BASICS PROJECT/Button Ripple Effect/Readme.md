# Button Ripple Effect

## 📌 Project Description

This project creates a **Button Ripple Hover Effect** using **HTML, CSS, and JavaScript**.
When you move your mouse over the button, a ripple animation appears from the cursor position.

---

## 🛠 Technologies Used

* HTML
* CSS
* JavaScript

---

## 📂 Project Structure

```
│── index.html
│── style.css
│── function.js
```

---

## 🚀 How It Works

* `index.html` creates the button structure. 
* `style.css` designs the button and ripple animation using `::before`. 
* `function.js` detects mouse position and sets CSS variables for ripple effect. 

When the mouse moves over the button:

* JavaScript gets the cursor position.
* It sets `--xPos` and `--yPos`.
* CSS expands a circle from that position.

---

## ▶️ How to Run

1. Download all files.
2. Keep them in the same folder.
3. Open `index.html` in your browser.
4. Hover over the button.

---

## ✨ Features

* Smooth ripple animation
* Dynamic mouse position effect
* Simple and clean design
* Beginner friendly

